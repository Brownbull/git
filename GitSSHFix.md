# Fix ssh for git
# Create public key 
# Open gitbash
$ ssh -keygen
enter 
enter
# go to folder where the key was created
$ cd C:\Users\${Your_User}\.ssh
# Open id_rsa.pub file copy key and add it to your Git Keys
